window.Counting = function(selector, base, number){
  var container = window.querySelector(selector);
  if(!container) return alert('Can\'t find ' + selector);

  base = base || 10;
  number = number || 0;

  
};
